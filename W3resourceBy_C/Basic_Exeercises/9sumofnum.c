#include <stdio.h>
#include <math.h>
#include <conio.h>
int main()
{
    int a;
    int b;
    scanf("%d%d", &a, &b);
    int sum = a + b;
    printf("Sum of the two number:%d ", sum);
    return 0;
}